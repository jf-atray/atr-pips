/*use crate::anim::{AnimId, AnimLibId};

#[derive(Debug, Clone, Copy)]
pub struct LivingAnimLib {
    pub lib: AnimLibId,
    pub root_anim: AnimId,
}

impl Default for LivingAnimLib {
    fn default() -> Self {
        Self {
            lib: AnimLibId::default(),
            root_anim: AnimId::default(),
        }
    }
}

#[derive(Debug, Clone, Copy)]
pub struct TumbleweedAnimLib {
    pub lib: AnimLibId,
    pub root_anim: AnimId,
}

impl Default for TumbleweedAnimLib {
    fn default() -> Self {
        Self {
            lib: AnimLibId::default(),
            root_anim: AnimId::default(),
        }
    }
}
*/
